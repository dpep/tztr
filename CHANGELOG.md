###  Unreleased

- Add `-j/--json` and `-J/--ndjson` structured output (`{original, detected_format, detected_tz, translated}` per match)
- Add `--detect` to report detected format/zone without converting
- Add `-d/--date` reference date for time-only inputs (resolves DST; accepts flexible formats)
- `-v` is now verbose mode; use `-V/--version` for version

###  0.1.0  (2026-04-19)

